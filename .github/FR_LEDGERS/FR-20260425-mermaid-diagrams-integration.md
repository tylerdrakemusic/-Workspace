# FR-20260425-mermaid-diagrams-integration — Mermaid Diagrams Integration (replaces Unified Benchmarks panel)

<!-- Created by ⊕workspace-intake. Header is updated in place by intake / CI only.
     Event Log and Artifacts are APPEND-ONLY for all agents. -->

## Header

- **FR ID:** FR-20260425-mermaid-diagrams-integration
- **Title:** Mermaid Diagrams Integration (replaces Unified Benchmarks panel)
- **Type:** feature
- **Risk:** low (replaces a single dashboard panel, not the portal)
- **Projects:** ⊕Workspace
- **State:** BRANCHED
- **Branch:** feature/workspace/mermaid-diagrams-integration
- **PRs:** #35 (draft)
- **Cycle timer:** 5252e153-6c3e-4305-953b-1e4901fa84f6
- **Opened:** 2026-04-25
- **Last updated:** 2026-04-25
- **Merged at:** —
- **Signed off at:** —
- **Closed:** —
- **Final state:** —

### Acceptance Criteria
1. New `f:\⊕Workspace\diagrams\` folder created with auto-generated seed `.mmd` files for all 5 projects (architecture + tech-stack + DB schema each) plus workspace-level diagrams (cross-project integrations, FR flow, agent topology).
2. New `f:\⊕Workspace\src\integrations\mermaid\` integration module: wraps mermaid.ink HTTP API with local `mmdc` CLI fallback (try local first → fall back to HTTP). Importable from any project.
3. Generator script `f:\⊕Workspace\tools\diagrams_dashboard.py` scans `diagrams/`, renders each `.mmd` to `reports/diagrams/*.svg`, and produces a browsable `reports/diagrams_dashboard.html` index page.
4. Diagrams dashboard page includes a **built-in live editor** (textarea + live SVG preview) so Tyler can edit/prototype diagrams in-browser without leaving the portal.
5. The new diagrams dashboard **replaces the `unified-benchmarks` panel** in `dashboard.json` (currently `reports/benchmark_dashboard.html` generated by `tools/bench_dashboard.py`). The existing portal.html and other dashboards are NOT touched.
6. `dashboard.json` registry updated: remove/deprecate the `unified-benchmarks` entry, register the new `diagrams` dashboard in its place.
7. `open_portal.ps1` is unchanged — the existing unified `portal.html` continues to serve as the entry point and now lists the new diagrams dashboard in place of the old benchmarks panel.
8. Tests under `⊕Workspace/tests/` cover: mermaid integration (HTTP + CLI fallback paths, mocked), generator script (scans `.mmd`, writes SVG, builds index HTML), and dashboard registry updates.

### Concurrency Notes
- Conflicts with: none (active FRs are all ❤Music-scoped — `band-mgmt-data-reconcile`, `guitar-trainer-db-migration`, `guitar-trainer-data-to-db`)
- Depends on: none

### Deliverable Tracker

<!-- Mutable table. Agents flip their own row's Status + Proof + Updated in place.
     Status vocab: not-started → in-progress → blocked → done → verified.
     Proof column: proof_artifact id (from proof_cli) or PR comment URL. -->

| #   | Deliverable                                        | Owner   | Status      | Proof | Updated |
| --- | -------------------------------------------------- | ------- | ----------- | ----- | ------- |
| AC1 | `diagrams/` folder + auto-generated seed .mmd set  | TBD     | not-started | —     | —       |
| AC2 | `src/integrations/mermaid/` module (HTTP + CLI)    | TBD     | not-started | —     | —       |
| AC3 | `tools/diagrams_dashboard.py` generator + index    | TBD     | not-started | —     | —       |
| AC4 | Built-in live editor on dashboard                  | TBD     | not-started | —     | —       |
| AC5 | Replace `unified-benchmarks` panel with diagrams   | TBD     | not-started | —     | —       |
| AC6 | `dashboard.json` registry update (swap entry)      | TBD     | not-started | —     | —       |
| AC7 | `open_portal.ps1` unchanged; portal lists diagrams | TBD     | not-started | —     | —       |
| AC8 | Tests for integration, generator, registry         | TBD     | not-started | —     | —       |

### Tyler's Original Request
> https://mermaid.js.org/config/usage.html — new FR I would like to consider setting up some diagrams to visualize the projects, the architecture, the technology, current state. I love working with .mmd mermaid files to visualize. Can we integrate with their api in workspace integrations, perhaps create an architecture folder or diagrams folder in workspace to get visuals on the project?

### Tyler's Confirmed Scope (intake interview, 2026-04-25)
- **Render strategy:** Both — local `mmdc` CLI preferred, mermaid.ink HTTP as fallback.
- **Seed diagrams:** Auto-generate full seed set (architecture + tech-stack + DB schema for all 5 projects + workspace-level diagrams).
- **Portal placement:** **Replace the `unified-benchmarks` panel** (not the portal itself). Existing `portal.html` and `open_portal.ps1` are unchanged. The benchmarks dashboard at `reports/benchmark_dashboard.html` is being retired in favor of the new diagrams dashboard.
- **Live editor:** Yes — add built-in live editor (textarea + live SVG preview) to the dashboard, in addition to VS Code Mermaid Preview extension support.

### Scope correction (2026-04-25, post-intake)
- Tyler clarified: replacement target is the **`unified-benchmarks` panel**, NOT the unified portal. Risk dropped from medium → low. AC5 / AC6 / AC7 rewritten accordingly. Original intake mistakenly inferred portal-level replacement from the freeform answer to Q3.

---

## Event Log

<!-- APPEND-ONLY. Newest entries at the bottom. Never edit past entries. -->

### 2026-04-25T00:00:00Z — ⊕workspace-intake

**Event:** state-transition

**Summary:** FR opened, interview run, scope confirmed by Tyler → TRIAGED

**Details:**
- Scope: ⊕Workspace (single repo)
- 4-question interview completed via vscode_askQuestions; all answers captured in Header → "Tyler's Confirmed Scope"
- Notable scope expansion from Tyler's freeform answer on Q3: replace unified portal & consolidate duplicates between `portal.html`, `agent_ops_dashboard.html`, and quantum panel
- Acceptance criteria drafted (8 ACs covering: seed diagrams, integration module, generator, live editor, portal replacement, registry update, launcher, tests)
- Concurrency check: clean — no active FR touches `⊕Workspace/reports/portal.html`, `dashboard.json`, `src/integrations/`, or `tools/`
- Risk: medium — replacing the unified portal touches Tyler's primary daily entry point; tests + visual sanity check required before merge

**Next:** awaiting Tyler: approve scope → hand off to ⊕workspace-ci for branch creation

---

### 2026-04-25T00:30:00Z — ⊕workspace-overseer (scope correction)

**Event:** scope-correction

**Summary:** Tyler corrected — replace `unified-benchmarks` panel only, not the portal

**Details:**
- Replacement target narrowed: `unified-benchmarks` dashboard registered in `dashboard.json` → `reports/benchmark_dashboard.html` (generated by `tools/bench_dashboard.py`)
- `portal.html`, `agent_ops_dashboard.html`, and quantum panel are NOT touched by this FR
- Risk lowered: medium → low
- AC5, AC6, AC7 rewritten
- Tyler's Confirmed Scope updated with explicit "Scope correction" note
- State remains TRIAGED — awaiting fresh approval against the corrected scope

**Next:** awaiting Tyler: approve corrected scope → hand off to ⊕workspace-ci

---

### 2026-04-25T01:00:00Z — ⊕workspace-ci

**Event:** state-transition

**Summary:** Tyler approved corrected scope → branch + draft PR created → BRANCHED

**Details:**
- Branch `feature/workspace/mermaid-diagrams-integration` created from `main` at SHA `9e55c15`
- Pushed to `origin` with upstream tracking set
- Draft PR opened: https://github.com/tylerdrakemusic/-Workspace/pull/35
- PR body summarizes all 8 ACs from this ledger
- Note: local `main` is 5 commits ahead of `origin/main` (band-mgmt portal-refresh commits). Direct push to `main` rejected by branch protection (correct CI behavior). Those 5 commits will appear in this PR's diff until they get their own PR merged. Flagged for follow-up.
- Cycle timer `5252e153-6c3e-4305-953b-1e4901fa84f6` continues running

**Next:** handoff to `⊕workspace-overseer` for implementation kickoff

---

## Artifacts

<!-- APPEND-ONLY. Links to concrete evidence. -->

- **Perf runs:** 5252e153-6c3e-4305-953b-1e4901fa84f6 — fr-cycle-FR-20260425-mermaid-diagrams-integration
- **Proof artifacts:** —
- **PRs:** #35 (draft) — https://github.com/tylerdrakemusic/-Workspace/pull/35
- **Commits:** —
- **Reports / dashboards:** target outputs — `reports/diagrams/*.svg`, `reports/diagrams_dashboard.html` (will replace `reports/portal.html` as unified entry)
