# Parallel test execution

The canonical command is `python tools/run_tests.py`. It loads `tools/parallel_test_policy.json`, composes its excluded markers with any `pytest.ini` `addopts` marker expression, emits concise output, and returns pytest's nonzero status on any worker failure. Serial and `--parallel` modes use the same exclusions; `--parallel` is only a reviewed local evidence run and requires `pytest-xdist`.

The serial run was attempted from branch head `00f1cdc9f600d0c4542a2ca1131a2ea14e083c72` on 2026-08-29 UTC with `python tools/run_tests.py --junitxml tmp/serial-baseline.xml`. At 18:04:53Z to 18:06:14Z, pytest completed 963 JUnit test records: 949 passed, 1 failed, 0 errors, and 13 skipped; the process exited 1. The failure was the pre-existing `tests/test_humanizer_skill.py::test_humanizer_source_is_byte_identical_to_declared_sha256` provenance hash mismatch. This is not a clean comparable baseline and is not evidence that parallel execution is safe. CI remains serial and pending a clean rerun. Existing `playwright` and `live` exclusions are unchanged. `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy.

At the actual current Workspace worktree head `00f1cdc9f600d0c4542a2ca1131a2ea14e083c72` on 2026-08-29 UTC, the bounded serial rerun used `C:\G\python.exe tools/run_tests.py --junitxml tmp/repair-serial.xml` and the bounded parallel rerun used `C:\G\python.exe tools/run_tests.py --parallel --junitxml tmp/repair-parallel.xml`. Each JUnit report contains 959 records: 951 passed, 1 failed, 0 errors, and 7 skipped; both processes exited 1 with the same failure identity, `tests/test_humanizer_skill.py::test_humanizer_source_is_byte_identical_to_declared_sha256`, expected SHA-256 `14fc8a965b6e0a8dc100ba4dffeab55cb94bbac112abbde7e014d5c15a35c202`, observed `5fd504ab8c910a4e59418fae143f6f7c6928822ffedf45fb525a9b3bb8d7a83b`. The humanizer source and test are unrelated and were not changed. These results are not evidence that parallel execution is safe; parallel safety remains unapproved and CI remains serial. `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy. Playwright tests remain excluded unless `PLAYWRIGHT_ENABLED=1`.

Serial fallback: `C:\G\python.exe tools/run_tests.py --junitxml tmp/serial.xml`.